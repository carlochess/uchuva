#!/bin/bash
#PBS -q batch
#PBS -l walltime=24:00:00
#PBS -o {{= it.dagdir}}/{{= it.nodo.directorio}}/{{= it.nodo.nombre}}.out
#PBS -e {{= it.dagdir}}/{{= it.nodo.directorio}}/{{= it.nodo.nombre}}.err
#PBS -N {{= it.nodo.directorio}}_{{= it.nodo.nombre}}
{{? it.nodo.dependencia.length > 0 }}#PBS -W depend=afterok:{{= it.nodo.dependencia.join(":")}}{{?}}
{{= it.config.location.trim() }} {{= it.config.argumento.trim() }}
####cd $PBS_O_WORKDIR
